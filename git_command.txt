git init
# git add.
# git commit -m "you message goes here"
# git status
# git log (hit 'q' to exit)
# git checkout (unique command id)

Git Remote Commands...
# git remote add origin (url)
# git remote -v
# git push origin master
# git pull origin master
# git clone (url)

Create new branch...
# git checkout -b home_page (to create and get into the the new branch)
# git checkout master (before merge go to master first)
# git merge (Your branch name) (will update master from branch)
# git push origin master 
# git branch -d (Your branch name) ...Once you are certain you do not need the branch anymore